<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>K-means Cluster Playlist Maker</title>
  <link rel="icon" href="/favicon.svg" type="image/svg+xml"/>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;600;700,800&display=swap" rel="stylesheet">
</head>

<body>
  <header>
    <h1>K-means Cluster Playlist Maker </h1>
  </header>
  <div class="container">
    <div class="input-graph-container">
      <div class="text-intro">
        <h2>This is a site that generate clusters according to your liked songs, want to try? </h2>
      </div>
      <div class="form-component">
        <input id="cluster-number" type="number" placeholder="Type the number of clusters" />
        <button id="submit">Yes, I do</button>
      </div>
      <h2 id="error">{{ error }}</h2>
      <div>
        <div id="graph"></div>
        <div id="loading">
        </div>
      </div>
    </div>
    <div id="clusters-container" />
  </div>
</body>
<script>
  {% include 'main/utils.js' %}
  {% include 'main/token.js' %}
  {% include 'main/index.js' %}
  {% include 'main/clusters.js' %}
</script>
<style>
  :root {
  --main-bg-color: #3e036b;
  --alt-bg-color-1: #121212;
  --alt-text-color: var(--alt-bg-color-1);
  --alt-bg-color-2: #1C036B;
  --alt-bg-color-3: #030C6B;
  --alt-bg-color-4: #6B0341;
  --main-text-color: white;
  }
  * {
    margin: 0;
    padding: 0;
  }

  body {
    background-color: var(--main-bg-color);
    color: var(--main-text-color)
    font-family: var(Montserrat, sans-serif);
  }
  h1 {
    font-size: 800;
  }
  header {
    height: 10vh;
    align-items: center;
    justify-content: center;
    display: flex;
    background-color: var(--alt-bg-color-4);
    border-radius: 0 0 100px;
      box-shadow: 11px 9px 20px 5px var(--alt-bg-color-4);

  }
  input {
    background-color: var(--alt-bg-color-1);
  }

  .form-component {
    display: flex;
    justify-content: space-around;
    align-items: center;
    width: 100%;
  }
  #cluster-number {
    border-radius: 500px;
    height: 100%;
    text-align: center;
    font-size: large;
  }
  .input-graph-container {
    align-items: center;
    display: flex;
    flex-direction: column;
  }
  button {
    border-radius: 500px;
    padding: 1em;
    color: var(--alt-bg-color-1);
    background-color: var(--main-text-color);
    font-size: medium;
    font-weight: 700
  }

  #graph {
    max-width: 32em;
    max-height: 32em;
    margin: 3em;
  }

  .graph {
    width: 100%;
    max-height: 100%;
    border-radius: 100px;
  }

  .text-intro {
    display: flex;
    flex-direction: column;
    height: 10vh;
    justify-content: space-around;
  }

  .container {
    display: flex;
    flex-direction: row;
    justify-content: space-evenly;
    flex-wrap: wrap;
  }

 /* width */
::-webkit-scrollbar {
  width: 10px;
}

/* Track */
::-webkit-scrollbar-track {
  background: #f1f1f1;
  border-radius: 10rem;
  box-shadow: inset 0 0 5px grey;

}

/* Handle */
::-webkit-scrollbar-thumb {
  background: #888;
    border-radius: 10rem;

}

/* Handle on hover */
::-webkit-scrollbar-thumb:hover {
  background: #555;
}

  {% include 'main/clusters.css' %}
  {% include 'main/loading.css' %}

</style>

</html>