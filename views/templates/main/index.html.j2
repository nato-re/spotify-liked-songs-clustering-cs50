<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Title</title>
</head>

<body>
  <div class="container">
    <div>
      <h1>Welcome to K-means Cluster Playlist Maker </h1>
      <p>This is a site that generate clusters according to your liked songs, want to try? </p>
      <button id="submit">Yes, I do</button>
      <label>
        <input id="cluster-number" type="number" placeholder="Type the number of clusters" />
      </label>
      <h2 id="error">{{ error }}</h2>
      <div>
        <div id="graph"></div>
        <div id="loading">
        </div>
      </div>
    </div>
    <div id="clusters-container">
    </div>
  </div>
</body>
<script>
  {% include 'main/utils.js' %}
  {% include 'main/token.js' %}
  {% include 'main/index.js' %}
  {% include 'main/clusters.js' %}
</script>
<style>
  * {
    margin: 0;
    padding: 0;
    color: white
  }
  body {
    background-color: black;
  }
  button, input {
    background-color: black;
  }
  #graph{
    max-width: 32em;
    max-height: 32em;
  }
  .graph{
    width: 100%;
    max-height: 100%;
  }

  .cluster-list{
    padding: 0.3em;
  }

  .cluster-list-item{
    list-style: none;
    margin: 0.5em;
  }

  .container {
    display: flex;
    flex-direction: row;
    justify-content: space-evenly;
  }

  .clusters-container {
    width: 30vw;
    display: flex;
    flex-direction: column;
    align-items: start;
  }

  .cluster-details {
    padding: 0.5em;
    background-color: rgb(72, 72, 72);
    border-radius: 1em;
    margin: 0.2em;
  }

  details[open] {
    padding: 1em;
    height: 10em;
    width: 30vw;
    overflow-y: scroll;
  }
details[open] summary{
  position:absolute;
  bottom:-10px;
  right:0%;
}
  .lds-ripple {
    display: inline-block;
    position: relative;
    width: 80px;
    height: 80px;
  }

  .lds-ripple div {
    position: absolute;
    border: 4px solid white;
    opacity: 1;
    border-radius: 50%;
    animation: lds-ripple 1s cubic-bezier(0, 0.2, 0.8, 1) infinite;
  }

  .lds-ripple div:nth-child(2) {
    animation-delay: -0.5s;
  }

  @keyframes lds-ripple {
    0% {
      top: 36px;
      left: 36px;
      width: 0;
      height: 0;
      opacity: 0;
    }

    4.9% {
      top: 36px;
      left: 36px;
      width: 0;
      height: 0;
      opacity: 0;
    }

    5% {
      top: 36px;
      left: 36px;
      width: 0;
      height: 0;
      opacity: 1;
    }

    100% {
      top: 0px;
      left: 0px;
      width: 72px;
      height: 72px;
      opacity: 0;
    }
  }
</style>

</html>